<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".HertzApp"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="Hertz-DS"
        android:theme="@style/Theme.HertzDS"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.HertzDS">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          The offline Piper/sherpa-onnx TTS engine runs in its own process. It is a
          third-party native (JNI/C++) library; if it ever segfaults, this isolates
          the crash to a disposable background process instead of taking down the
          whole app. The main process detects the disconnect and falls back to the
          system TTS engine.
        -->
        <service
            android:name=".voice.VoiceEngineService"
            android:process=":voice"
            android:exported="false" />
    </application>
</manifest>
